### Изучение роли Python в области исследования космического пространства

### изучите и установите средства, необходимые для обучения программированию;
### познакомитесь с основными понятиями программирования и сможете применить их к реальным задачам NASA;
### узнаете о передовых технологиях, таких как машинное обучение и искусственный интеллект;
### получите советы и послушаете истории о работе от самих сотрудников NASA.

### Ознакомьтесь с типами задач, связанных с изучением космического пространства, в решении которых могут помочь Python, а также обработка и анализ данных.


### Введение
### Узнайте о возможностях программы "Артемида"
### Сведения о текущих задачах в исследованиях космического пространства
### Ознакомьтесь со STEM благодаря возможностям НАСА
### Достигните большего благодаря вдохновению от космических полетов
### Использование программирования, обработки и анализа данных для решения задач, связанных с исследованием космического пространства
### Проверка знаний
### Сводка

### Установка средств программирования для разработки на Python
- Что такое язык программирования
- Установка Visual Studio Code и преимущества этого редактора кода
- Установка расширений для улучшения возможностей Visual Studio Code
- Установка Python и почему это популярный язык программирования
- Запуск базового Jupyter Notebook в Visual Studio Code
### Узнайте о том, что такое программирование, и установите средства, которые помогут вам программировать.
```
Обозреватель: в нем показано, какие файлы открыты, а также содержимое открытой папки
Поиск: служит для поиска определенного слова или набора слов в файле
Система управления версиями: помогает писать код и работать над ним совместно с другими пользователями
Запустить: служит для запуска написанного вами кода
Расширения: здесь отображаются все расширения, добавленные людьми со всего мира. Доступны различные цветовые темы, языки программирования и многое другое. Вы можете установить и настроить Visual Studio Code по своему усмотрению.
Azure: дополнительное расширение, помогающее интегрировать Microsoft Azure в Visual Studio Code
Docker: на этом уроке данный раздел не рассматривается
```
### Написание простого кода Python в Записных книжках в Visual Studio Code
```
как и когда следует использовать комментарии;
как использовать математические функции в Python;
как создавать переменные;
как использовать готовые функции;
как выводить данные.
```
### Изучите основы Python.

### Введение
### Упражнение. Добавление комментариев в Записные книжки Python для пояснения кода
### Упражнение. Реализация простейших арифметических действий в коде Python для будущего анализа данных
### Упражнение. Создание переменных в Записных книжках Python для будущего анализа данных
### Упражнение. Создание текста и операции с ним в коде Python
### Упражнение. Создание списков данных в коде Python
### Упражнение. Просмотр результатов переменных и выражений в Записных книжках Python
### Проверка знаний
### Сводка
### XP: 1000

### Операторы управления кодом в Python
```
Как создавать и когда использовать условные выражения
Как создавать и когда использовать циклы while и for
Как создавать собственные функции
Как вызывать собственные функции
Как использовать параметры и глобальные переменные
```
### Изучите сложные вопросы, связанные с Python, более подробно.

### Введение
### Упражнение. Сравнение значений данных с помощью кода Python
### Упражнение. Управление потоком кода с помощью условных операторов
### Упражнение. Повторение кода с циклами while и for
### Упражнение. Разделение кода на фрагменты по функциям для удобного повторного использования и удобочитаемости
```
def OutputRocketText():
    global rocketText 
    rocketText = "We will launch in"
    rocketText = rocketText + " two days"
OutputRocketText()    
print(rocketText)
# We will launch in two days
```
### Упражнение. Импорт данных из текстовых файлов с помощью Python
```
file = open("b4042.txt", encoding="utf8")
for line in file:
    print(line)

```
### Упражнение. Использование параметров для указания вариативности в функциях
### Проверка знаний

### Подсчет количества лунных метеоритов по типам с помощью Python
```
Как создавать переменные в контексте программы
Как использовать готовые функции для чтения данных из текстового файла
Как создать функцию и вызвать ее в контексте программы
Как создавать условные выражения и циклы в контексте программы

```
### Используйте Python и Visual Studio Code, чтобы создать простую программу для подсчета метеоритов по типам.
```
Reading Rocks
basalt
breccia
highland
regolith
highland
breccia
highland
regolith
regolith
basalt
highland
basalt
breccia
breccia
regolith
breccia
highland
highland
breccia
basalt
```
### Введение
### Упражнение. Создание записной книжки Jupyter Notebook для изучения данных о лунных метеоритах
### Упражнение. Считывание данных о лунных метеоритах из текстового файла для изучения
### Упражнение. Создание функции для подсчета типов найденных лунных метеоритов
```
1. Как мы изменили число переменных для космических пород в созданной функции?

Мы передали их в качестве параметров.

Мы сделали их глобальными переменными. v

2. Как мы назвали функцию countMoonRocks?

для rock в rockList: countMoonRocks(rock) v

для rockList в rock: countMoonRocks(rockList)

while rock: countMoonRocks(rock)

while rockList: countMoonRocks(rock)

3. Какой код мы вызвали по окончании считывания данных из файла rocks.txt?

.finish()

.close() v

.end

.done()

```
### Упражнение. Подсчет количества лунных метеоритов посредством вызова функции Python

```
items = []
with open("b4042.txt") as file:
    for item in file:
        items.append(item.strip())
items = items[1:]
oneItems = []
for item in items:
    if oneItems.count(item) == 0:
        oneItems.append(item)
countItems = []
for item in oneItems:
    countItems.append([item])

for item in countItems:
    item.append(items.count(item[0]))

sortedItems = sorted(countItems, key = lambda x: -x[1])


print(sortedItems)
print(countItems)
print(oneItems)
print(items)

```

### Упражнение. Анализ и сведение данных в Python
- Моделирование проблем с использованием понятий ООП.
- Описание основных понятий, используемых в ООП.
- Отличие классов от объектов.
### Проверка знаний
### Сводка

### Введение в объектно-ориентированное программирование на языке Python
### Что такое объектно-ориентированное программирование?
```
class Square:
  __height = 0

  def __init__(self):
    self.__height = 2
    self.__width = 2

  def set_side(self, new_side):
    self.__height = new_side
    self.__width = new_side

  def method(self):
    print(f"{self.__height} {self.__width}")


square = Square()
square.set_side(4)
square.__height = 3 # raises AttributeError
print(square.__height)
square.method()

# 3
# 4 4


square = Square()
square.set_side(4)
square._Square__height = 3 
# print(square.__height)
square.method()

# 3 4

```

```
class Square:
  def __init__(self):
    self.__height = 2
    self.__width = 2
  def set_side(self, new_side):
    self.__height = new_side
    self.__width = new_side
  def get_height(self):
    return self.__height
  def set_height(self, h):
    if h >= 0:
      self.__height = h
    else:
      raise Exception("value needs to be 0 or larger")

square = Square()
square.set_height(-2)
```

```
class Square:
  def __init__(self, w, h):
    self.height = h
    self.__width = w
  
  def set_side(self, new_side):
    self.__height = new_side
    self.__width = new_side

  @property
  def height(self):
    return self.__height

  @height.setter
  def height(self, new_value):
    if new_value >= 0:
      self.__height = new_value
    else:
      raise Exception("Value must be larger than 0")

square = Square(2,3)
print(square.height)

```


### Использование классов и переменных для превращения модели ООП в код
«Камень, ножницы, бумага» — это игра для двух участников. Игра состоит из раундов. В каждом раунде участник выбирает символ камня, ножниц или бумаги, и второй участник делает то же самое. Победитель раунда определяется путем сравнения выбранных символов. По правилам игры камень бьет (затупляет) ножницы, ножницы бьют (режут) бумагу, а бумага бьет (покрывает) камень. Победитель раунда получает очко. Игра может длиться сколько угодно раундов. Победителем становится участник, набравший больше всего очков.
### Упражнение. Моделирование и создание игры
```
class Participant:
  def __init__(self):
    self.points = 0
    self.choice = ""

class GameRound:

class Game:
   def __init__(self):
     self.endGame = False
     self.participant = Participant()
     self.secondParticipant = Participant()


```
### Добавление поведения с помощью методов
```
import random

class Participant:
  def __init__(self, name):
    self.name = name
    self.items = ["rock", "paper", "scissor"]
    self.points = 0
    self.choice = 0

  def choose(self):
    self.choice = random.randint(0, 2)
    print(f"{self.name} select {self.items[self.choice]}")

  def getChoice(self):
    return self.choice

  def incrementPoint(self):
    self.points += 1


class GameRound:
  def __init__(self, p1, p2):
    self.rules = [
      [ 0, -1,  1],
      [ 1,  0, -1],
      [-1,  1,  0]
    ]
    p1.choose()
    p2.choose()
    result = self.compareChoices(p1, p2)
    if result > 0:
      p1.incrementPoint()
    elif result < 0:
      p2.incrementPoint()

    print(f"Round resulted {p1.name} is {self.getResultAsString(result)}")
    print(f"Game ended, {p1.name} has {p1.points}, and {p2.name} has {p2.points}")

  def compareChoices(self, p1, p2):
    return self.rules[p1.getChoice()][p2.getChoice()]
  
  def getResultAsString(self, result):
    res = {
      0: "draw",
      1: "win",
      -1: "loss"
      }
    return res[result]

class Game:
  def __init__(self):
    self.participantFirst  = Participant("Spock")
    self.participantSecond = Participant("Kirk")
    self.endGame = False

  def start(self):
    while not self.endGame:
      gameRound = GameRound(self.participantFirst, self.participantSecond)
      self.checkEndCondition()

  def checkEndCondition(self):
    answer = input("Continue game y/n <y>")
    answer = 'y' if answer.strip() == '' else 'n'
    if answer == 'y':
      self.endGame = False

    else:
      self.endGame = True

game = Game()
game.start()

# Game ended, Spock has 5, and Kirk has 3
# Continue game y/n <y>
# Spock select rock
# Kirk select paper
# Round resulted Spock is loss
# Game ended, Spock has 5, and Kirk has 4
# Continue game y/n <y>
# Spock select rock
# Kirk select scissor
# Round resulted Spock is win
# Game ended, Spock has 6, and Kirk has 4
# Continue game y/n <y>

```
### Упражнение. Добавление поведения в игру

### Упражнение. Добавление новых правил в реализацию игры

### Проверка знаний
```
1. Чем отличается класс от объекта?

Это одно и то же.

Класс — это схема. Объект — это конкретный экземпляр, который создается на основе схемы.

Класс содержит данные, а объект — нет.

Класс имеет методы, а объект — нет.
2. Как в Python реализуются методы доступа?

Он использует префиксы. Один символ подчеркивания, _, в начале имени указывает, что эта переменная защищена. Два символа подчеркивания, __, указывают, что переменная является частной, и при присвоении вызывают исключение.

Он использует ключевые слова private и public.

Все содержимое класса в Python по умолчанию скрыто и должно предоставляться путем добавления методов, возвращающих значения переменных.

Добавьте декоратор @public или @private, чтобы сделать элемент общедоступным или частным.
3. Выберите лучшее объяснение для ключевого слова self.

Это ключевое слово, ссылающееся на экземпляр объекта. Можно также использовать ключевое слово this.

Ключевое слово self ссылается на базовый класс объекта.

Ключевое слово self используется только для ссылки на члены объекта и передается как параметр.

Ключевое слово self ссылается на экземпляр объекта. Переменные, являющиеся частью экземпляра объекта, должны быть назначены self. Ключевое слово self также передается в каждый метод класса, которому требуется доступ к экземпляру объекта.


```
### Сводка

